{
  "name": "webwiki",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix sitewiki",
    "dev": "npm run dev --prefix sitewiki",
    "build": "npm run build --prefix sitewiki",
    "start": "npm run start --prefix sitewiki",
    "lint": "npm run lint --prefix sitewiki"
  }
}
